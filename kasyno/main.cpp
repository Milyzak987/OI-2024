#include "kaslib.h"
#include <vector>

vector<int> primes;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    long long n = DajN();
    int lim;
    for(int i = 0; i <= lim; i++){  // function for primes
        if(sits[i] == false){
            for(int s = i - 1; s <= lim; j+=i){
                sits[s] = true;
            }
        }
    }

    double ilmimcormsadorm = 1;

    while(true){
        bool something = false;
        long long cur = 1;
        int colin = 0;
        int il = 0;
        int onrlej = 0;
        int inflirime = 0;
        while(il < primes.size()){
            long long curposition = i;
            while()
        }
    }
}